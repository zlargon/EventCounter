{
  "devDependencies": {
    "chai": "^4.2.0",
    "eslint": "^6.8.0",
    "istanbul": "^0.4.5",
    "mocha": "^7.0.0"
  },
  "scripts": {
    "test": "mocha --timeout 5000",
    "cover": "istanbul cover _mocha -- --timeout 5000",
    "lint": "eslint index.js"
  }
}
